import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertTrue;

public class TicketTest {
    @Test
    public void testProjectRuns() {
        // A simple test to prove the CI pipeline can run tests
        assertTrue(true);
    }
}